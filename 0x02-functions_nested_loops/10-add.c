#include "main.h"
/**
 * add -adds two integers together
 * @a: value to be used for sum.
 * @b: second value to be use fro sum.
 *
 * Return: int (sum)
 */
int add(int a, int b)
{
	return (a + b);
}

