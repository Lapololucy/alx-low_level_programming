#include "main.h"
/**
 * _abs - return the absolte value of a given integer.
 * @n: value use to find the absolute value.
 *
 *
 * Return: the absolute vale.
 */
int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}

